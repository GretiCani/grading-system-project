server:
  port: 9999
spring:
  application:
    name: configuration-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: src/main/resources/config
        bootstrap: true
